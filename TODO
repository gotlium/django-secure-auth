Remove all user sessions on all devices
Remove used code from available
Secure code to email
Authentication by ssl certificate
Check defined settings access on views
Send email, if password was changed
Send email with code for modify any secureauth settings
Show captcha if http-agent was not set
Change password form protection
Confirm code by email if some settings was enabled

Registration:
    add "I agree terms of use" to registration form
    reset password by sms + link
    auto auth, when activation complete

Refactoring:
    sign & unsign, instead of Sign().sign() & Sign().unsign()
    check ip & user agent, when user registering new auth method

clean_auth_trash - management command
trust browser. add to settings how many days user can use browser without auth


Readme
Docs
Tests
Live demo
table on codes confirmation
